# For nvim config
npm
nodejs
python
unzip
clang
clang-tools-extra
rustup
go
fzf
fd
lazygit
tree-sitter-cli
meson

# Terminals
foot
kitty

# Linux Packages
base linux linux-firmware
intel-ucode/amd-ucode
sudo
git
base-devel
grub
timeshift
networkmanager
reflector
man
openssh
xorg-xinit 

# Git config
git config --global user.email "TsuBenn@github.com"
git config --global user.name "TsuBenn"

# Keymap
keyd (sudo systemctl enable keyd --now - /etc/keyd/default.conf)
```
[ids]

*

[main]

capslock = esc

esc = `
```

# Hyprland
hyprland
qt6-wayland
xdg-desktop-portal-hyprland

# Fonts
sans-serif
noto-fonts 
noto-fonts-emoji
noto-fonts-cjk
noto-fonts-extra

# Audio drivers
wireplumber
pipewire pipewire-alse pipewire-jack pipewire-pulse
pavucontrol
(REMEMBER TO ENABLE VIA SYSTEMCTL >> systemctl --user enable pipewire.service...)
EasyEffects (REMEMBER lsp-plugins)

# Nvidia Drivers
nvidia (nvidia-dkms if linux-zen is used)
sudo mkinitcpio -P (rebuild initramfs)
nvidia-utils 
lib32-nvidia-utils
linix-headers
cuda (This thing is heavy, careful)
libva-nvidia-driver
opencl (Get the necessary ones)

# Linux Zen
linux-zen linux-zen-headers
sudo grub-mkconfig -o /boot/grub/grub.cfg


# AUR
git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si && cd

# Multilib
sudo vim /etc/pacman.conf -> uncomment multilib and the line below it

# Menu
rofi
rofi-emoji
(REMEMBER TO PROPERLY GENERATE LOCALE VIA LOCALE-GEN >> sudo locale-gen)
wtype
wl-clipboard

# Essentials Apps
firefox
discord
obs-studio
7zip
zoxide

# Fix Default Apps running thing?
archlinux-xdg-menu


# Media
pinta (Image Editing Software)
mpv
yazi
dolphin
 - XDG_MENU_PREFIX = arch- kbuildsyscoca6
 - env = XDG_MENU_PREFIX, arch- (hyprland.conf)
vlc
vlc-plugin-x264
vlc-plugin-ffmpeg
alac wavpack lame libmad

# Gaming
mesa
portproton *
gamemode (optional)
gamescope (optional)
wine

# Monitoring
iftop
htop
atop
btop (This looks the best)
nvtop

# Unikey
fcitx5 fcitx5-im fcitx5-configtool
fcitx5-unikey #Vietnamese
fcitx5-mozc   #Japanese
(
REMEMBER TO SET THE ENV VAR:
- export GTK_IM_MODULE=fcitx
- export QT_IM_MODULE=fcitx
- export XMODIFIERS=@im=fcitx
)

# Clipboard
clipvault *
wl-clipboard

# Uxplay (iPad Screen Sharing)
uxplay *
gstreamer
gst-plugins-good gst-plugins-bad gst-libav gstreamer-vaapi
(REMEMBER TO START avahi-daemon USING systemctl)

# Android screen Sharing
scrcpy
adb *


# Notification
swaync

# Ricing
quickshell
swww (for now)
ttf-jetbrains-mono-nerd
qt6-base qt6-declarative qt6-svg qt6-wayland

# Cool Aesthetic CLI stuff
cava
cbonsai *

# Flatpak Apps
Kdenlive

# Calculator
Qalculator *

# Lossless Scaling
lsfg-vk *
